# VERIFY-SPEC — 결정 7건 근본해결 + 추가 5건 (2026-09-19)

## 구현 결정

- **`section` 재분류 대신 `modeScope` 신설.** 노트 id가 `note:{patch}:{section}:{slug}:{내용해시}`라
  section을 바꾸면 id가 바뀌고, 커밋된 두 델타 파일의 `matchedNoteIds`가 전부 댕글링되며
  `candidateSetHash`(전체 노트 직렬화)가 달라져 LLM 캐시 388건이 무효가 된다. 새 필드는
  `toCandidateView`가 직렬화하지 않아 캐시·id가 모두 불변이다.
- **노트는 재파싱하지 않고 마이그레이션.** 라이엇이 26.18 페이지를 발행 후 수정해(저장본 180 →
  오늘자 162) 재파싱은 관측 시점 사실을 파괴한다. `scripts/run-migrate-notes.ts`가 필드만 덧붙이고,
  modeScope 외 변경이 감지되면 기록을 중단한다.
- **LLM 교정은 후보 풀이 아니라 검증 지점에서.** 풀을 거르면 candSetHash가 바뀌어 유료 전량
  재호출이 된다. `verifyCauses`/`verifySummaryCites`가 비-core 인용을 기각한다(회색 처리).
- **프롬프트 v4는 정확성 수정과 분리해 별도 단계로 했다(초안에서 번복).** 처음에는 "이번 라운드에
  하지 않는다"로 잡았다 — 문구만 바꾸고 `PROMPT_VERSION`을 안 올리면 캐시된 답이 그것을 만들지
  않은 프롬프트에 귀속되고, 올리면 캐시 전량 무효 + 채점된 문장이 비결정적으로 교체되기 때문이다.
  사용자 지시가 "전건 근본해결"이었고 1차 재매칭 실지출이 45콜(약 $1 규모)로 확인돼 비용 근거가
  약해졌으므로, **정확성 수정을 먼저 커밋해 안전지대를 만든 뒤**(8a10b46) 별도 커밋(bcd174c)으로
  적용했다. 기계적 절반(길이·완곡 종결 집계 → `meta.llm.prose`)은 그대로 들어가 있다.
  결과는 `PLAN-root-fix-2026-09-19.md` §4-2 참고 — 원인 문장 80자 초과 0건, 검증 통과 원인 증가.
- **`display-normalize.ts` 존치(멱등 가드로 강등).** 재생성 후 입력은 0건이지만, 노트만 갱신되고
  델타가 옛 판정으로 남는 중간 상태가 실제로 가능하다(재매칭은 로컬 전용, Vercel은 커밋본을 읽는다).
- **죽은 앵커는 되살리지 않고 뗐다.** `#discord` 섹션 제거는 2026-09-14 사용자 지시였다.
- **PUBG 원천 블록은 지우지 않고 접었다.** "모든 판정문은 원천 링크를 가진다"가 불변식이라,
  자연어 문장을 위로 올리고 파일 경로·매치 id는 `<details>`로 내렸다.

## 테스트 정합성 — 명세 변경 보고 2건

1. `src/components/home/__tests__/render.test.tsx`의 DiscordPanel 케이스: "마지막 전송"·"디스코드로
   브리핑 보내기"를 고정하고 있었으나 **그 문구가 거짓이었다**(정적 사이트는 전송하지 못하고,
   값은 델타 파일 생성 시각이며, "공지"는 방송 대상이 아니다). 통과시키려 고친 것이 아니라 문구가
   틀려서 함께 갱신했다.
2. 픽스처 팩토리 다수: `PatchNoteItem`에 필수 필드가 생겨 `modeScope`를 추가했다. 모드 앵커를 쓰는
   픽스처는 앵커에서 파생시킨다(고정값 `"core"`를 두면 모드 케이스가 조용히 core로 테스트된다 —
   실제로 처음 일괄 추가했을 때 display-normalize 테스트 7건이 그 이유로 깨졌다).

## 미확인 사항

- ~~프롬프트 v4 미적용~~ → **적용 완료**(위 구현 결정 참고). 26.17→26.18 기준 요약 100자 초과 2건
  (최장 107자) · 원인 80자 초과 0건(최장 72자)으로 닫혔다. 수치는 `meta.llm.prose`에 기록된다.
- `data/raw/26.18/matches.jsonl`이 없어 `evidence.matchIds` 1936건은 이전 파일에서 승계했다.
  원천 링크는 유지되지만 이번 재생성이 그 값을 새로 계산한 것은 아니다.
- 홈 "기타 변경" 블록의 스킨 이미지 복구는 실렌더로 확인해야 한다(정적 자산 존재는 빌드 타임에
  page.tsx가 확인하지만, 새 렌더 경로에서 실제로 그려지는지는 별개).
- PUBG 맵 상세에는 근거 블록이 없다(무기 상세에만 있었다). 자연어 근거는 무기 상세에만 적용했다.

## 실렌더 확인 결과 (2026-09-19, 로컬 정적 빌드 http://127.0.0.1:4330)

- PUBG 무기 상세: 근거가 자연어 4문장 + "판정 규칙 보기 →" + 접힌 "원천 데이터 보기"로 바뀌었다.
- PUBG 내비: `/pubg/compare/`·`/pubg/methodology/`로 정확히 링크된다(회귀 해소).
- 홈 기타 변경: 카테고리 4블록(버그 수정·신규 스킨·치장·증강·게임 모드(클래식))이 테두리+제목
  밴드로 분리됐고 스킨 스플래시 **9장**이 다시 렌더된다. 클래식 65줄은 스킬 단위로 합쳐져 41행.
  예: "W - 응수 기본 지속 효과: … · 사용 시: …"(이전엔 2행).
- 대조표: 카사딘 선택 시 `role="status"` toast가 뷰포트 하단 16px에 뜬다(문구 동일, 4초 자동 소멸).
- 대조표 라인 선택: "아이템은 라인별로 집계하지 않아 라인을 고르면 이 표에서 빠집니다." 노출.

## 알려진 한계 (이번 라운드에 고치지 않음 · 근거 있음)

- **26.18 클래식 섹션 줄의 엔티티가 전부 "피오라"다.** 그 섹션 markup은 h4가 챔피언명이 아니라
  스킬명이라 파서가 챔피언 경계를 잃는다(26.16·26.17은 "신규 아칼리"처럼 정상 분리된다).
  판정에서는 modeScope로 전부 빠졌고, 표시에서는 엔티티 묶음이 1개라 **머리글을 달지 않으므로
  거짓 라벨이 나가지 않는다**(각 줄의 본문이 챔피언명을 말한다). 근본 교정은 재파싱이 필요한데
  그건 26.18 노트 19건을 영구 소실시킨다(§구현 결정 참고).

## 게이트 판정 대응 (2026-09-19)

### scope-critic — 화면 (`DECISION_CHANGED: yes`)
- **로고가 `gameHref(game, "/")`를 쓴다** → **반영.** 출력 URL은 같지만 의미가 "게임 전환"이라
  같은 함정이 남는다. `sectionHref(game, "")`로 교체했고, 이제 `gameHref` 호출부는 게임 드롭다운
  하나뿐이다.
- **toast key 증가 여부 · skinPreviews 전달 여부(확인 불가)** → **실측으로 확인됨.**
  `CompareExplorer`가 선택마다 `selectSeq`를 올려 `key`를 바꾸고, `ReleaseNoteStream`이
  `skinPreviews`를 넘긴다. 실렌더에서 toast 노출과 스킨 9장을 확인했다.
- **엔티티가 1개인 카테고리에서 머리글이 사라진다** → **의도.** 카테고리 라벨과 같은 말을 두 번
  하게 되고, 특히 26.18 클래식은 파서가 모든 줄을 "피오라"로 귀속해 머리글을 달면 **거짓 라벨**이
  된다(위 "알려진 한계").

### scope-critic — 파이프라인 (`DECISION_CHANGED: yes`)
- **새 모드가 규칙표에 없으면 core로 떨어져 원래 버그가 재발한다** → **부분 반영 + 사유.**
  검토 결과 재발하지 않는다: 모르는 h2 제목은 `resolveSectionStrategy`의 기본값 `system`으로
  떨어지고, `entity-match`는 champion|item 버킷만 만들므로 SR 델타와 짝지어질 경로가 없다.
  오귀속이 가능했던 것은 "클래식"이 **명시적으로** 하위 라벨을 champion/item으로 재분류하는
  분기를 가졌기 때문이다. 이 안전한 기본값을 테스트로 고정했다(`patchnotes-parser.test.ts`).
  **남는 잔여 위험**: 그런 노트가 `system`으로 LLM 후보 풀에 들어간다(체계·버그 수정 노트와
  구분할 구조 신호가 없다). 규칙표 갱신이 필요하다는 프로세스 의존은 인정한다.
  파싱 시점에 던지는 불변식도 넣어 봤다가 **되돌렸다** — 유일하게 도달 가능한 경로가 "h3 앵커
  없는 정상 SR 챔피언 블록"이라, 라이엇 마크업이 조금 달라지면 파이프라인 전체가 죽는다.
  조용한 오분류보다 나쁜 실패다. 커밋 데이터 불변식 검사(테스트)는 그대로 둔다.
- **`mappingFailures`에 세지 않아 관측 가능성을 잃는다** → **사유를 남기고 유지.** 그 카운터는
  "ddragon 키로 해석하지 못한 엔티티"를 뜻한다. 모드 노트는 해석 실패가 아니라 대상 밖이라
  섞으면 그 지표가 두 가지를 말하게 된다. 관측은 `notes-invariants` 테스트와 마이그레이션
  스크립트의 스코프별 집계 출력이 맡는다.
- **다른 소비자 전수 확인(미결)** → **확인 완료.** `notes-count.ts`는 `isDisplayExcludedNote`
  경유로 이미 반영된다. `indirect-effect.ts`·`verdict.ts`는 이미 걸러진 causes·matchOutcome만
  보고, `cosmetic-note.ts`·`sectionBundle.ts`는 표시 분류라 스코프와 무관하다.
- **필수 필드 여부(미결)** → 필수다(`src/pipeline/types.ts`). optional이면 새 모드 노트가 조용히
  core 기본값을 먹는다.

### security-auditor — Critical 0 · Warning 1
- **`run-migrate-notes.ts`가 `type: "string"`으로 patch를 받는다** → **반영.** 다른 모든 파이프라인
  스크립트와 같이 `type: "patch"`로 바꿔 형식 검증을 걸었다.

## acceptance-critic 판정 대응 (2026-09-19, UNMET 5 · UNKNOWN 7)

- **V3 문서 모순(프롬프트 v4 미적용 기술)** → **반영.** 위 "구현 결정"과 "미확인 사항"을 갱신해
  PLAN §4-2와 같은 말을 하게 했다. 지적이 정확했다 — 번복 자체는 PLAN에 기록했으나 이 파일은
  옛 결정을 그대로 두고 있었다.
- **V2 수용 기준 `meta.llm.calls === 0`이 최종 데이터와 불일치** → **반영.** PLAN §4의 그 기준에
  적용 범위(1차 재매칭 한정)를 명시했다. 최종 커밋 데이터의 120·110콜은 §4-2 단계의 정상 산출이다.
- **V5 라인 결과 0건 빈 상태(확인 불가)** → **실재하는 결함이었고 고쳤다.**
  `ReleaseNoteStream.tsx`의 빈 상태 분기가 `filtered.length === 0 && miscTotal === 0`이라, 라인을
  골라 결과가 0건이어도 기타 변경이 있으면 **안내 없이 무관한 블록만** 남았다. 목록 머리에
  한 줄로 말하게 바꿨다(기타 변경 블록은 라인 축이 없으므로 그대로 둔다).
- **V7 맵 상세에 근거 블록 없음** → **반영(형태는 다르게).** 맵에는 통계 판정 행이 없어 무기와 같은
  "판정 근거" 문단이 성립하지 않는다. 대신 **표본 출처 한 문장**을 넣었다(어느 맵의 몇 건을
  집계했는지 + 패치노트에 맵 항목이 없다는 사실). 없는 판정을 있는 것처럼 서술하지 않는 쪽이
  이 프로젝트 원칙과 맞다.
- **V1 노트 diff 순수성(확인 불가)** → **닫음.** `git show 8a10b46 -- data/aggregated/notes/`에서
  `modeScope` 추가 줄과 `anchorKind` 줄의 쉼표 변경을 제외하면 남는 차이가 없다. 구조 비교
  (파싱 후 필드별 대조)로도 세 파일 전부 0건이었다.
- **V4 `verify.sh --full`(확인 불가)** → **닫음.** Spec·tsc·ESLint·vitest·빌드·design-lint 전항목
  통과 로그가 있다(스크래치패드 `verify-full-2.log`).
- **V6 `data/raw`·`.env`·키 미커밋(확인 불가)** → **닫음.** 커밋 3건의 `--name-only`에 해당 경로가
  한 건도 없다.

## 독립 채점 92/100 보완 대응 (2026-09-19)

- **K2-7 방법론 수치가 stale(가장 무거운 지적)** → **반영.** "대상 113 / 못 찾음 36 / 신뢰도 191·33·3"은
  v4 재생성 **이전** 값이었다. 같은 라운드의 마지막 단계가 데이터를 바꿨는데 화면이 옛 수를
  계속 말한 것이다. `computeLlmCauseStats`(순수 함수, 테스트 보유)로 빌드 타임에 델타에서 산출한다.
- **K3-4 `deltas/*.json` 불변식 테스트 부재** → **반영.** `deltas-invariants.test.ts` 신설. 수정 전
  데이터로 대조하면 모드 노트 짝 **925·858건**, 모드 인용 원인 **282·18건**으로 실패한다 —
  발화하지 않는 규칙이 아니라 결함을 그 형태 그대로 인코딩한 게이트다.
- **K1-2 "방송 규칙 보기 →"의 도착지에 방송 이야기가 없음** → **반영.** 방법론에 "디스코드로 무엇이
  나가나"(보내는 것 / 보내지 않는 것 / 언제 / 마지막 집계의 의미)를 넣고 CTA가 그 지점을 가리키게
  했다. 2026-09-14에 제거된 것은 **미리보기 목업**이고 이번에 넣은 것은 사실 서술이라 사용자 지시와
  충돌하지 않는다.
- **K1-3 어댑터 표 "맵 9종"이 실제 7종과 불일치** → **반영.** "맵 관측 7종(자산 9종)"으로 갈라 적었다.
- **K1-7 요약 길이 계측이 프롬프트 상한과 어긋남** → **반영.** `SUMMARY_MAX_CHARS=100`·
  `CAUSE_MAX_CHARS=80`으로 분리해 규칙과 계측이 같은 숫자를 본다.
  **잔여**: 요약 5건이 100자를 넘긴다(최장 110자). 이건 모델의 규칙 미준수라 재생성 없이는 닫히지
  않는다 — 다음 자연 재생성(26.19)에서 다시 측정한다.
