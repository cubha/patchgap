# VERIFY-SPEC R3-G1 — 노트→델타 best-row 역색인

- 대상 파일: src/components/home/noteDeltaIndex.ts, src/app/page.tsx, src/components/home/releaseStream.ts
- 구현 결정:
  - 선택 규칙 ①보고 가능(유의+바닥) ②STATUS_SORT_PRIORITY ③|Δ|. 통과 행 없으면 아무 행이라도 유지
  - page.tsx와 sortMatchedGroups가 같은 함수를 호출(사전 불일치 방지)
- 부수 결정: ReleaseNoteRow의 스킬 행도 이 사전을 받으므로 스킬 행 배지가 유의 행 기준으로 바뀔 수 있음(의도)
- 미확인 사항: 프로덕션에서 관측 보유 엔티티 수(시뮬레이션 7/20)와 스킬 행 배지 변화는 배포 후 실측
- 검증 방법: noteDeltaIndex.test.ts 4건 · verify.sh --full · 라운드3 프로덕션 프로브
