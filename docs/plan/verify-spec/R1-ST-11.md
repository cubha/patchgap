# VERIFY-SPEC R1-ST-11 — PUBG main 랜드마크·링크 밑줄·제목 break-keep

- 대상 파일: src/app/pubg/**/page.tsx, src/components/pubg/shared.tsx
- 구현 결정:
  - 각 return 분기의 Container를 <main>으로 감쌈
- 부수 결정:
  - 없음
- 미확인 사항:
  - Lighthouse a11y 재측정은 배포 후
- 검증 방법: `bash verify.sh --full` + 해당 __tests__ + 프로덕션 재채점(SCORECARD §3 라운드2)
